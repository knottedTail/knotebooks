# `process.proof_idea`

## Purpose

A `process.proof_idea` atom records a strategic outline of a proof.

Use when:

- the general mechanism is understood
- details remain unwritten or unchecked

Examples:

- `Reduce to the rank-one case and then lift by functoriality.`
- `Interpret the center geometrically and compare dimensions.`
