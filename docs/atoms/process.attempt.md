# `process.attempt`

## Purpose

A `process.attempt` atom records a trial reasoning step toward a goal.

Use when:

- trying an approach
- testing a proof strategy
- pushing an argument forward without full certainty

Examples:

- `Try induction on the filtration degree.`
- `Attempt to prove surjectivity by dimension count.`
