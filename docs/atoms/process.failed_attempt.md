# `process.failed_attempt`

## Purpose

A `process.failed_attempt` atom records a trial path that breaks down.

Use when:

- a proof strategy does not work
- a required condition fails
- a hoped-for reduction is invalid

Examples:

- `This induction fails because the filtration is not preserved.`
- `The argument breaks at the root-of-unity specialization.`
