# `process.example`

## Purpose

A `process.example` atom records a concrete mathematical instance used for testing or illustration.

Use when:

- checking a definition in a special case
- testing a conjecture
- illustrating a phenomenon

Examples:

- `Take U_q(sl_2) at a primitive l-th root of unity.`
- `Consider the polynomial ring k[x].`
